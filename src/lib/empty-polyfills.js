// Intentionally empty. Aliased in next.config.ts to replace Next's
// unconditional `polyfill-module` (core-js) bundle. Our browserslist targets
// modern browsers, so these polyfills are dead weight flagged by PageSpeed as
// "legacy JavaScript". See lesson: nextjs_unconditional_polyfills.
export {};
